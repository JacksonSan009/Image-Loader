import { Routes } from '@angular/router';
import { CustomerGalleryComponent } from './components/customer-gallery/customer-gallery.component';

export const routes: Routes = [
  { path: '', component: CustomerGalleryComponent }
];
